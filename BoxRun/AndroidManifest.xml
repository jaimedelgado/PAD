<?xml version="1.0" encoding="UTF-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.boxrun"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="landscape"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Pantalla_principal"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:theme="@style/FullscreenTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- created by ADT ext plugin at 2015-05-10 16:22:31 -->
        <!--  -->
        <activity android:name=".activity_ajustes" />
        <!-- created by ADT ext plugin at 2015-05-10 16:28:55 -->
        <!--  -->
        <activity android:name=".Ajustes" />
        <activity
            android:name=".Pantalla_ajustes"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_pantalla_ajustes"
            android:theme="@style/FullscreenTheme" >
        </activity>
        <activity
            android:name=".Pantalla_tienda"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_tienda"
            android:theme="@style/FullscreenTheme" >
        </activity>
-->
        <activity
            android:name=".Pantalla_juego"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_pantalla_juego"
            android:theme="@style/FullscreenTheme" >
        </activity>
    </application>

</manifest>
